---
title: realpath
description: Learn how to use the realpath command in Linux to print the resolved path.
---
# realpath
The `realpath` command is used to print the resolved path by expanding all symbolic links and resolving references to `/./`, `/../` and extra `/` characters.
